$(function ($) {
    // 引入文件
    // 结构化 HTML设置data-stellar-background-ratio="0.1" css 设置background-attachment: fixed;
    // js 初始化
    $.stellar({
        horizontalScrolling: false,
        responsive: true
    });
})